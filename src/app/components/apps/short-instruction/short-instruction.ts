import { Component } from '@angular/core';
import { SectionTitle } from "../section-title/section-title";

@Component({
  selector: 'app-short-instruction',
  imports: [SectionTitle],
  templateUrl: './short-instruction.html',
  styleUrl: './short-instruction.scss'
})
export class ShortInstruction {

}
