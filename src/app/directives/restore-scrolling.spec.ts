import { RestoreScrolling } from './restore-scrolling';

describe('RestoreScrolling', () => {
  it('should create an instance', () => {
    const directive = new RestoreScrolling();
    expect(directive).toBeTruthy();
  });
});
